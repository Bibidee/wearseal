import Link from 'next/link';
export default async function Passport({params}:{params:Promise<{id:string}>}){const {id}=await params;return <main className="wrap"><nav className="nav"><Link className="wordmark" href="/">WEARSEAL<span className="orange">/</span></Link><span className="mono">PASSPORT {id}</span></nav><section className="hero"><div><div className="mono orange">AUTHORITATIVE CONTRACT READ</div><h1>Equipment passport.</h1><p>The live agreement, commitment hashes, verdict and vault state will render here after the wallet adapter is connected to the deployed Studionet addresses.</p><div className="grid"><Link className="plate card" href={`/a/${id}/baseline`}>BASELINE PROOF →</Link><Link className="plate card" href={`/a/${id}/return`}>RETURN PROOF →</Link><Link className="plate card" href={`/a/${id}/inspect`}>INSPECTION →</Link></div></div></section></main>}
