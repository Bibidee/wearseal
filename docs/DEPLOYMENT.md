# Deployment

Target only Studionet chain 61999 (`https://studio.genlayer.com/api`). Pin `genlayer-js` to `1.1.8`. Compile and lint both Python contracts, deploy Agreement then Vault, bind the published addresses, and record real receipts. A funded signer is required for live deployment; no address or hash is fabricated by this repository.
