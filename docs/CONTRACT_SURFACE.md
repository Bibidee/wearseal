# Contract surface

Agreement: `get_agreement`, `accept_baseline`, `submit_return`, `inspect`, `settlement_instruction`, `mark_settled`. Vault: `deposit`, `get_vault`, `settle`, `refund_cancelled`. Deployment must bind addresses once and record source hashes, tx hashes, execution results, and final readbacks.
