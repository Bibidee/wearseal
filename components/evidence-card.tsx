import HashDNA from './hash-dna';
export default function EvidenceCard({kind,url,hash}:{kind:string;url:string;hash:string}){return <article className="evidence-card"><div className="evidence-image"><img src={url} alt={`${kind} evidence`}/><span className="verified-mark">✓</span></div><div className="evidence-copy"><div className="eyebrow">{kind} EVIDENCE <span className="verified">HASH VERIFIED</span></div><a className="evidence-url" href={url} target="_blank" rel="noreferrer">{url}</a><div className="hash-row"><HashDNA hash={hash}/><code>{hash.slice(0,10)}…{hash.slice(-8)}</code><button onClick={()=>void navigator.clipboard?.writeText(hash)} aria-label={`Copy ${kind} hash`}>COPY</button></div></div></article>}
