'use client';import {useWallet} from '../lib/wallet/provider';
export default function WalletBar(){const w=useWallet();if(!w?.account)return <button className="wallet-button" onClick={w?.connect}>CONNECT WALLET</button>;return <div className="wallet-bar"><span className={`network-dot ${w.onStudionet?'':'wrong'}`}/><span>{w.onStudionet?'STUDIONET 61999':'WRONG NETWORK'}</span><span>{w.account.slice(0,6)}…{w.account.slice(-4)}</span>{w.error&&<span className="orange">{w.error}</span>}</div>}
