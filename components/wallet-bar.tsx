'use client';import {useWallet} from '../lib/wallet/provider';export default function WalletBar(){const w=useWallet();return <div className="mono" style={{display:'flex',gap:12,alignItems:'center'}}>{w?.account?<>{w.onStudionet?'STUDIONET 61999':'WRONG NETWORK'} · {w.account.slice(0,6)}…{w.account.slice(-4)}</>:<button onClick={w?.connect}>CONNECT WALLET</button>}{w?.error&&<span className="orange">{w.error}</span>}</div>}
